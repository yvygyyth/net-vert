# 强制使用 pnpm
engine-strict=true

# 项目级固定官方 npm 源，避免受全局配置影响
registry=https://registry.npmjs.org

# 保存依赖时使用精确版本
save-exact=true

# 自动安装 peer dependencies
auto-install-peers=true

# 严格的 peer dependencies
strict-peer-dependencies=false

