{
  "name": "verify-release-npm-install",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "pnpm exec tsc --noEmit --strict --skipLibCheck --target ES2022 --module ESNext --moduleResolution Bundler ./smoke.ts",
    "smoke": "node --experimental-strip-types ./smoke.ts"
  },
  "dependencies": {
    "net-vert": "latest"
  }
}